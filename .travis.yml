language: node_js
node_js:
  - "10.0.0"
env:
  global:
    - SCOPE=headwood
  matrix:
    - PACKAGE=ts-build-template
    - PACKAGE=test-helper
install:
  - "cd ts-build-template; npm i"
script:
  - "cd ts-build-template; npm start cover.travis"
